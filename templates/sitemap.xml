<?xml version="1.0" encoding="UTF-8"?>
{% set datefmt = '%a, %d %b %Y %H:%M:%S GMT' %}
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  {% for item in site.slugs %}
  <url>
    <loc>{{ site.base_url }}{{ site.slugs[item].url }}</loc>
    {% if site.slugs[item].datetime %}
    <lastmod>{{ site.slugs[item].datetime.strftime(datefmt) }}</lastmod>
    <changefreq>daily</changefreq>
    {% else %}
    <changefreq>weekly</changefreq>
    {% endif %}
  </url>
  {% endfor %}
</urlset>
